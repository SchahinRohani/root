load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "uwebsockets",
    hdrs = glob([
        "src/**/*.h",
    ]),
    includes = ["src"],
    defines = [
        "UWS_NO_ZLIB",
    ],
    copts = [
        "-std=c++20",
    ],
    deps = [
        "@usockets//:usockets",
    ],
)
